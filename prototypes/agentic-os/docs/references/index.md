# References

LLM-readable reference materials for AgenticOS.

- Skill registry schema: `AI_Workspace/.codex/registry.json`
- Run log format: `outputs/run_log.jsonl` (append-only JSONL)
- Job state: `outputs/job_state.json` (atomic-rewrite per run)
